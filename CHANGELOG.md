# 變更紀錄

## 1.0.18 - 2026-08-23

### 修正與優化

- 修正手機版行程卡的距離／步行時間被單行省略的問題，改為可完整換行顯示。
- 距離與步行時間拆分為獨立顯示區塊；步行時間達 60 分鐘以上時改用「小時／分鐘」格式，例如 `100 分` 顯示為 `1 小時 40 分`。
- 修正行程摘要卡的天氣地點同步，優先顯示使用者在行程日設定的地點。
- 恢復「下一個活動」只尋找尚未到時間活動的邏輯，當日活動均已經過時則顯示尚未安排下一活動。
- 應用程式版本同步更新為 V1.0.18，並同步維護 package.json、package-lock.json、src/app/version.ts 與 README.md。

## 1.0.17 - 2026-08-23

### 新功能

- 新增 30 個韓國旅途服務可愛圖示，涵蓋交通、美食、景點、服裝與旅遊服務分類。
- 圖案選擇器新增關鍵字搜尋、最愛與最近使用篩選，並持久保存於 IndexedDB。
- 新增今日行程摘要卡，顯示下一個活動、天氣狀態、當日活動數與預算剩餘。
- 新增可離線開啟的唯讀 HTML 行程快照，保留日期、時間、地點、備註與行程圖示。
- 設定頁新增圖案設定匯入，可還原分類、名稱、最愛與最近使用偏好。

### 修正與優化

- 圖案偏好只保存有效的 catalog ID，會自動去除重複、未知與超出上限的最近使用項目。
- 舊版圖案設定匯入時，即使缺少最愛與最近使用欄位也能正常完成。
- 匯入圖案設定時會清理不存在的圖示 ID 與非法覆寫資料，避免污染圖案選擇器。
- 離線 HTML 快照會將可用的景點／活動圖示內嵌為 data URL，不依賴網路即可閱讀。

## 1.0.16 - 2026-08-23

### 新功能

- 新增 30 個亞洲著名景點的可愛風格圖示，依照 6 欄×5 列總圖切割為 30 個 512×512 WebP 素材。
- 將 30 個景點圖示整合至「設定 → 圖案分類管理 → 景點」圖庫，並依景點名稱建立可辨識的圖庫標籤。

### 修正與優化

- 應用程式版本同步更新為 V1.0.16，並同步維護 package.json、package-lock.json、src/app/version.ts 與設定頁版本顯示。
- 新增景點圖庫完整性測試，確保 30 個景點項目均有正確分類與圖片資產。

## 1.0.15 - 2026-08-23

### 新功能

- 新增依行程分類套用預設圖案與圖案分類分頁。
- 更新 Labnana 圖片生成流程，加入內建 GPT Image 2 備援並移除硬編碼金鑰。

### 修正與優化

- 全域文字基準調整為 14px。
- 優化圖案選擇器在新增行程時的分類預選行為。

## 1.0.14 - 2026-08-22

### 新功能

- 新增旅行應急 ZIP 備份，包含 HTML、CSV、JSON、照片與 LINE QR Code。
- 新增匯入前本機快照與最近一次快照還原。
- 新增 PWA 離線狀態與新版更新提示。
- 新增行程下一景點距離與步行時間估算。
- 手機支援長按行程卡片拖曳排序。

### 修正與優化

- 旅伴聯絡資訊改為預設收合。
- 維持行程時間排序，僅允許相同時間行程調整順序。
- 優化天氣卡片地點與當地時間的手機排版。
- 增加行程卡片間距與手機操作區域。
- 記帳搜尋框改為主題一致的圓角樣式。
